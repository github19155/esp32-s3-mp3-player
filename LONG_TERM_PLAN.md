# 长期功能规划

> 目的：维护项目长期方向，避免每次换 AI 或换阶段时把功能顺序做乱。
> 当前原则：小步开发、小步实机验证、小步提交；不要把多个大功能混在同一轮。

## 1. 文档分工

- `AI_HANDOFF.md`：当前进度、当前任务、交接状态。
- `CODEX_GUIDE.md`：Codex 的角色边界、评审规则、协作纪律。
- `LONG_TERM_PLAN.md`：长期路线、阶段边界、功能优先级。
- `HARDWARE_REFERENCE.md`：硬件事实和不能乱改的底层约束。

长期计划变化时，只改本文件；当前任务状态变化时，同步 `AI_HANDOFF.md`。

## 2. 总路线

```text
MP3 稳定基线
-> WiFi 系统能力
-> NTP 时间
-> WiFi 上传 MP3
-> 设置页 + 状态栏
-> MP3 体验完善
-> BLE / IMU / 录音
-> 摄像头
```

优先级原则：

1. 先做会被后续功能依赖的系统能力。
2. 先做能解决当前真实痛点的功能。
3. 高风险硬件功能单独开分支，不混入主线。
4. 每个阶段都要能编译、能烧录、能实机验证。

## 3. 阶段规划

### 阶段 0：MP3 稳定基线

状态：基本完成，`master` 保持 MP3 v0.1 稳定基线。

已完成：

- LCD + LVGL + 触摸。
- SD 卡读取。
- MP3/WAV 播放、暂停、切歌、音量。
- 中文界面。
- `/sdcard/music` 优先扫描，回退 `/sdcard`。

近期不做：

- 歌词。
- 封面。
- ID3 解析。
- EQ。
- 复杂播放模式。

### 阶段 1：WiFi 系统能力

状态：当前主线阶段，分支 `feature/wifi`。

已完成：

- 扫描附近 WiFi。
- 点击 SSID 输入密码。
- 手动连接 WiFi。
- 显示连接状态和 IP。
- 保存成功连接的 SSID/password 到 NVS。
- 自动连接保存过的网络。

下一步：

- 忘记网络/清除保存：清除 `wifi_cfg/ssid`、`wifi_cfg/password`、`wifi_cfg/has_saved`。
- 验证退出 WiFi 页面后联网状态是否保持。
- 验证重启后自动连接失败时，手动扫描连接仍可用。

完成标准：

- 编译通过。
- 扫描、手动连接、保存、自动连接都实机通过。
- 忘记网络后重启不再自动连接旧网络。
- 退出 WiFi 页面后，后续 NTP/上传功能仍能使用网络。
- `AI_HANDOFF.md` 已更新并提交到 `feature/wifi`。

暂时不做：

- NTP。
- MP3 上传。
- 网页服务。
- 云服务。
- BLE。

### 阶段 2：NTP 时间能力

目标：让设备拥有可靠时间，为主菜单显示、上传记录、日志、后续设置页打基础。

功能：

- WiFi 已连接后自动进行 NTP 同步。
- 主菜单显示当前时间。
- 同步失败时显示未同步状态，不影响 MP3 和 WiFi。
- 保留最后一次同步时间或启动后的运行时间提示。

完成标准：

- 有网时能同步时间。
- 断网时界面不崩溃、不阻塞。
- 主菜单时间显示不遮挡现有 UI。
- 不改变 MP3 播放逻辑。

### 阶段 3：WiFi 上传 MP3

目标：解决“不能方便往 SD 卡放歌”的痛点。

优先方案：局域网页面上传。

功能：

- ESP32 启动一个简单 HTTP 服务。
- 手机/电脑访问设备 IP。
- 上传 `.mp3` / `.wav`。
- 文件保存到 `/sdcard/music`。
- 上传完成后能刷新 MP3 列表。

完成标准：

- 上传小文件和常见 MP3 文件成功。
- 文件名冲突有明确处理方式。
- SD 卡不存在或空间不足时有提示。
- 上传时不破坏当前播放状态，必要时先要求停止播放。

暂时不做：

- 云盘同步。
- 外网访问。
- 用户账号。
- 复杂文件管理后台。

### 阶段 4：设置页 + 状态栏

目标：把功能集合整理成更像系统的体验。

功能：

- 顶部状态栏：WiFi 状态、时间、SD 卡状态、播放状态。
- 设置页：WiFi 管理、亮度、音量、关于设备。
- 清除保存网络入口可放入设置页。
- 显示固件版本、当前分支/构建信息。

完成标准：

- 状态栏不遮挡已有页面。
- 设置项能保存到 NVS 的才保存，不做假入口。
- 页面返回逻辑一致。

### 阶段 5：MP3 体验完善

目标：在不破坏 v0.1 基线的前提下逐步增强播放器。

可选功能：

- 记忆上次播放歌曲和音量。
- 歌曲排序。
- 删除歌曲。
- 播放列表刷新。
- 简单循环/顺序播放。

暂缓功能：

- 歌词。
- 封面。
- ID3。
- EQ。

### 阶段 6：BLE / IMU / 录音

目标：利用开发板已有硬件扩展可玩性。

候选功能：

- BLE 遥控播放/暂停/切歌/音量。
- QMI8658 姿态页面、水平仪、小交互。
- ES7210 录音为 WAV，保存到 SD 卡。

原则：

- 每个功能单独小阶段。
- 先做最小可验证版本。
- 不和 WiFi 上传、NTP 混在一起做。

### 阶段 7：摄像头

状态：高风险，最后做，必须单独分支。

原因：

- 当前 `CAMERA_EN=0`。
- BSP 中启用摄像头会触发 `#error`。
- 摄像头涉及 SCCB/I2C、PSRAM、LCD 显示、内存压力。

开始条件：

- MP3、WiFi、NTP、上传、设置页都稳定。
- 单独创建摄像头分支。
- 先跑通官方例程或最小预览，再考虑集成。

## 4. 近期任务队列

按顺序做，不要跳：

1. 补 WiFi 忘记网络/清除保存。
2. 验证 WiFi 第三阶段：保存、自动连接、退出保持联网。
3. 更新 `AI_HANDOFF.md`，封版 WiFi 第三阶段。
4. 开始 NTP 时间同步。
5. NTP 稳定后，再做 WiFi 上传 MP3。

## 5. 分支建议

- `master`：稳定基线，只合并实机验证通过的阶段。
- `feature/wifi`：当前 WiFi 系统能力。
- 后续建议：
  - `feature/ntp-time`
  - `feature/web-upload`
  - `feature/settings-status`
  - `feature/ble-remote`
  - `feature/imu`
  - `feature/recording`
  - `feature/camera`

任何分支合并到 `master` 前，必须由用户明确要求。
